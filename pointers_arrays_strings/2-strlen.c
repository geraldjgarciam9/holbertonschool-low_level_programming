#include <stdio.h>
#include <string.h>
/**
*_strlen - devuelve la longitud de cadena
*@s: almacena el valor acendente
*
*Return: el tamaño de la cadena
*/
int _strlen(char *s);
int _strlen(char *s)
{
return (strlen(s));
}
