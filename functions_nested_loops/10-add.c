#include <stdio.h>
/**
*add - adiciona dos numeros
*@a: variable de tipo entero
*@b: variable a sumar
*
*Return: devuelve el resultado en suma
*/
int add(int a, int b);
int add(int a, int b)
{
int suma = a + b;
return (suma);
}
